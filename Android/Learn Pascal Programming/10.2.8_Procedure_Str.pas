program Project1;
var
  S:string;
  i:real;

begin
  i:= -0.563;
  str(i,s);
  write(s);
  readln;
end.

{Procedure Str(Val:Integer/LonInt/Real; var s:string);
Description
Converts an integer or a decimal value to a string. The value parameter Val is
converted into a string and passed through the variable parameter S.}
